syntax = "proto3";

package exclave.core.app.dns.fakedns;
option csharp_namespace = "Exclave.Core.App.Dns.Fakedns";
option go_package = "github.com/exclavenetwork/exclave-core/v5/app/dns/fakedns";
option java_package = "com.github.exclavenetwork.exclave.core.app.dns.fakedns";
option java_multiple_files = true;

import "common/protoext/extensions.proto";

message FakeDnsPool{
  option (exclave.core.common.protoext.message_opt).type = "service";
  option (exclave.core.common.protoext.message_opt).short_name = "fakeDns";

  string ip_pool = 1; //CIDR of IP pool used as fake DNS IP
  int64  lruSize = 2; //Size of Pool for remembering relationship between domain name and IP address
}

message FakeDnsPoolMulti{
  option (exclave.core.common.protoext.message_opt).type = "service";
  option (exclave.core.common.protoext.message_opt).short_name = "fakeDnsMulti";

  repeated FakeDnsPool pools = 1;
}